# Existing
MONGODB_URI=
EMAIL_USER=
LINKEDIN_EMAIL=
LINKEDIN_PASSWORD=

# AWS SES
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
EMAIL_REPLY_TO=          # reply-to address (e.g. your Gmail); defaults to EMAIL_USER if unset

# Gmail connector (second Gmail account)
EMAIL_USER2=            # second Gmail address (e.g. spsourabh1428@gmail.com)
EMAIL_PASS2=            # Gmail App Password for EMAIL_USER2

# Resend connector
resend_api_key=         # Resend API key (from resend.com dashboard)
                        # From address is hardcoded: sourabh@referral.sourabhpathak.online

# New — Server
DASHBOARD_PASSWORD=
JWT_SECRET=
TRACK_SECRET=
DASHBOARD_ORIGIN=
TRACKING_WORKER_URL=

# New — Dashboard (Vite)
VITE_API_URL=

# New — Cloudflare Worker
EXPRESS_API_URL=
